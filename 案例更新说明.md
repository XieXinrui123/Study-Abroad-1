# DreamBridge 案例更新说明

以后更新案例不需要直接手改大段 JSON。推荐使用以下流程：

1. 打开 `cases-manager.html`。
2. 如果页面通过本地服务器访问，它会自动读取当前 `cases.json`；如果直接双击打开，请点击“导入”并选择现有 `cases.json`。
3. 少量更新可直接点击“新建案例”或选择已有案例编辑。
4. 批量更新时，先下载并填写 `案例批量导入模板.xlsx`，再导入管理器。
5. 处理校验错误后点击“校验并导出 JSON”。
6. 先备份网站根目录中的旧 `cases.json`，然后用新下载的文件替换它。
7. 刷新 `cases.html`，确认案例总数、筛选结果和详情展示正常。

## Excel 填写规则

- 同一位学生有多个 Offer 时，每个 Offer 单独占一行，并重复填写相同的“案例编号”。
- 请不要修改第 4 行表头名称，也不要在表头上方插入新行。
- 多个申请地区用 `/` 分隔，例如 `香港/英国`。
- 本科层次、学历和录取层次尽量使用模板中的下拉选项。
- `案例编号` 应为正整数，新增案例建议从当前最大编号继续。
- 管理器会根据多个 Offer 自动选择最高录取层次写入 `result`。

## 校验结果

- 红色“错误”会阻止导出，例如编号重复、缺少本科专业或录取院校字段不完整。
- 黄色“提醒”不会阻止导出，例如本科院校未公开、尚无 Offer、语言成绩未填写、录取专业待更新或疑似重复案例。
- 浏览器草稿只保存在当前设备和浏览器，不能代替文件备份。

## 文件安全

- 不要在公开案例中填写学生姓名、手机号、微信号、邮箱或其他敏感身份信息。
- 每次替换前建议把旧文件重命名为 `cases-YYYY-MM-DD.json` 保存一份。
- 管理器不会自动上传或发布数据；只有替换网站文件并重新部署后，公开页面才会变化。

## 推荐的本地打开方式

在网站目录打开终端并运行：

```bash
python3 -m http.server 8000
```

然后访问：

```text
http://localhost:8000/cases-manager.html
```

这样管理器可以自动加载当前 `cases.json`。使用完毕后在终端按 `Control + C` 停止服务器。
