# DreamBridge 案例更新说明

以后更新案例不需要直接手改大段 JSON。推荐使用以下流程：

1. 打开线上 `cases-manager.html`，页面会自动读取当前 `cases.json`。
2. 少量更新可直接点击“新建案例”或选择已有案例编辑。
3. 批量更新时，先下载并填写 `案例批量导入模板.xlsx`，再导入管理器。
4. 处理校验错误后，点击“发布到线上网站”。
5. 输入仅授权给 `XieXinrui123/Study-Abroad-1` 的 GitHub Fine-grained Token，再点击“保存并发布”。
6. 等待 GitHub Pages 更新，通常 1–2 分钟后刷新 `cases.html` 即可看到新案例。

## 首次创建发布令牌

1. 在 GitHub 账号设置中进入 Developer settings → Personal access tokens → Fine-grained tokens。
2. Repository access 选择“Only select repositories”，只选择 `Study-Abroad-1`。
3. Repository permissions 中仅将 Contents 设置为“Read and write”。
4. 建议设置有效期；创建后立即复制，因为 GitHub 不会再次完整显示该令牌。
5. 发布时把令牌粘贴到管理器。管理器不会把它写入代码、草稿或案例文件。

令牌属于账号凭据，请勿通过微信、邮件或截图发送给他人。若怀疑泄露，应立即在 GitHub 中撤销并重新创建。

## Excel 填写规则

- 同一位学生有多个 Offer 时，每个 Offer 单独占一行，并重复填写相同的“案例编号”。
- 请不要修改第 4 行表头名称，也不要在表头上方插入新行。
- 多个申请地区用 `/` 分隔，例如 `香港/英国`。
- 本科层次、学历和录取层次尽量使用模板中的下拉选项。
- `案例编号` 应为正整数，新增案例建议从当前最大编号继续。
- 管理器会根据多个 Offer 自动选择最高录取层次写入 `result`。

## 校验结果

- 红色“错误”会阻止导出，例如编号重复、缺少本科专业或录取院校字段不完整。
- 黄色“提醒”不会阻止导出，例如本科院校未公开、尚无 Offer、语言成绩未填写、录取专业待更新或疑似重复案例。
- 浏览器草稿只保存在当前设备和浏览器，不能代替线上数据或文件备份。

## 文件安全

- 不要在公开案例中填写学生姓名、手机号、微信号、邮箱或其他敏感身份信息。
- GitHub 会保留每次发布的提交记录，因此发布后的历史版本可以追溯。
- “校验并导出 JSON”仍可用于离线备份；建议在大量修改前导出一份。
- 发布令牌不会保存在浏览器草稿或本地存储中，关闭发布窗口后请妥善保管原令牌。

## 推荐的本地打开方式

在网站目录打开终端并运行：

```bash
python3 -m http.server 8000
```

然后访问：

```text
http://localhost:8000/cases-manager.html
```

这样管理器可以自动加载当前 `cases.json`。使用完毕后在终端按 `Control + C` 停止服务器。
